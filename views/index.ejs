<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pokédex</title>
  <link rel="stylesheet" href="/styles.css">
</head>

<body>
  <header>
    <h1><a href="/" style="text-decoration: none; color: inherit;">Pokédex</a></h1>
    <form method="GET" action="/">
      <input type="text" name="search" placeholder="Buscar Pokémon" value="<%= search %>" />
      <button type="submit">Buscar</button>
    </form>
  </header>
  
  <main>
    <div class="pokemon-grid">
      <% pokemons.forEach(pokemon=> { %>
        <a href="/pokemon/<%= pokemon.name %>" class="pokemon-card">
          <img
            src="https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/<%= pokemon.url.split('/').slice(-2, -1) %>.png"
            alt="<%= pokemon.name %>" />
          <h2>
            <%= pokemon.name %>
          </h2>
        </a>
        <% }) %>
    </div>
    <div class="pagination">
      <% if (currentPage> 1) { %>
        <a href="/?page=<%= currentPage - 1 %>&search=<%= search %>">Anterior</a>
        <% } %>
          <% if (currentPage < totalPages) { %>
            <a href="/?page=<%= currentPage + 1 %>&search=<%= search %>">Próxima</a>
            <% } %>
    </div>
  </main>
</body>

</html>